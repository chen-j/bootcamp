<ul>
	<% @projects.each do |project| %>

			<li><%= link_to project.name, project_path(project) %>
				<% if project.description.present? %>
					 – <%= project.description %>
				<% end %>
			</li>
	<% end %>
</ul>
