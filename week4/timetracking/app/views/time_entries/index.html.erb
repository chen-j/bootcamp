<h1><%= @my_project.name %></h1>

<ul>
	<% @my_entries.each do |entry| %>
		<li>
			<%= entry.hours %> Hrs
			<%= entry.minutes %> mins -
			<%= entry.date %>
		</li>
	<% end %>
</ul>
<%= link_to "Add new time entry", new_project_time_entry_path %>
